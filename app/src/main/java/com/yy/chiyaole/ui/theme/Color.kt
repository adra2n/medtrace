package com.yy.chiyaole.ui.theme

import androidx.compose.ui.graphics.Color

// 主色调：沉稳的青绿色，传达健康、安心
val Primary = Color(0xFF1A8E8C)      // 主青绿
val PrimaryLight = Color(0xFF5BC0BE) // 浅青绿
val PrimaryDark = Color(0xFF0F5F63)  // 深青绿

// 强调色：柔和暖沙色，与主色形成温和对比，不刺眼
val Accent = Color(0xFFC98A4B)       // 暖沙金
val AccentLight = Color(0xFFE2B585)  // 浅沙金
val AccentDark = Color(0xFF9C6630)   // 深沙金

// 背景色：偏冷的浅灰白，干净通透
val Background = Color(0xFFF6F8F8)    // 浅冷灰白
val Surface = Color(0xFFFFFFFF)       // 纯白
val BackgroundDark = Color(0xFFE1E7E7) // 深冷灰

// 文本颜色：带青绿调的深色，与主题呼应
val TextPrimary = Color(0xFF163234)    // 主要文本（深青墨）
val TextSecondary = Color(0xFF5A6B6E)  // 次要文本（中灰）
val TextDisabled = Color(0xFF9AA8A9)   // 禁用文本（浅灰）

// 功能色：用于特定状态的提示
val Success = Color(0xFF2E9E5B)  // 成功状态（绿）
val Error = Color(0xFFD64550)    // 错误状态（红）
val Warning = Color(0xFFC98A4B)  // 警告状态（沙金）

// 卡片底色：浅青绿，与页面背景形成柔和层次
val CardSurface = Color(0xFFEAF4F3)     // 浅色模式卡片底
val CardSurfaceDark = Color(0xFF223133) // 深色模式卡片底
